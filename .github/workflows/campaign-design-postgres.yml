name: Campaign design PostgreSQL contract

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  campaign-design-postgres:
    # RUNTIME_MUTATION_DISABLED=true: disposable campaign database validation is disabled until its workflow is admitted by the protected control-plane contract.
    if: ${{ false }}
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    services:
      postgres:
        image: postgres:17.6-alpine@sha256:ef257d85f76e48da1c64832459b59fcaba1a4dac97bf5d7450c77753542eee94
        env:
          POSTGRES_DB: campaign_design_test
          POSTGRES_USER: campaign_ci
          POSTGRES_PASSWORD: campaign-ci-disposable
        ports:
          - 127.0.0.1:5432:5432
        options: >-
          --health-cmd "pg_isready -U campaign_ci -d campaign_design_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - name: Check out the exact event source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Install locked test dependencies
        run: python -m pip install --require-hashes -r requirements-test.txt
      - name: Verify persistent campaign design behavior
        env:
          CAMPAIGN_DESIGN_TEST_DATABASE_URL: postgresql+asyncpg://campaign_ci:campaign-ci-disposable@127.0.0.1:5432/campaign_design_test
        run: python -m pytest -q tests/test_campaign_design.py
